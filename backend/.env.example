# Server
PORT=4000
JWT_SECRET=CHANGE_ME_generate_a_long_random_string

# Database (leave default unless deploying alongside another app that needs a different path)
DB_PATH=./data/tumya.db

# Payments
UPI_ID=ssstephenel@oksbi
UPI_PAYEE_NAME=Tumya
MOMO_NUMBER_PRIMARY=+256709877737
MOMO_NUMBER_SECONDARY=+256757244016

# Push notifications — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=mailto:admin@tumya.app
